/**
 * Shared API helpers for CLI and UI.
 * Contract bindings live in `contract/`; this package holds app-level types/utils.
 */

export * from './common-types.js';
export * as utils from './utils/index.js';
